import React from 'react'

const ButtonList = ({name}) => {
  return (
    <div className=''>
      <button className='bg-gray-200 px-5 py-2 m-2 rounded-lg'>{name}</button>
    </div>
  )
}

export default ButtonList